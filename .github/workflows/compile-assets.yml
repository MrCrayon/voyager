name: Compile Assets

on:
  push:
    paths:
    - '.github/**'

jobs:
  compile-assets:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Install Dependencies
      run: ls publishable/assets/js -l
